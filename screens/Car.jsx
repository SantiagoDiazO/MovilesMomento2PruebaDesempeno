import { Text, View } from 'react-native'

export default function Car({navigation}){
    return(
        <View>
            <Text style={{fontWeight:'bold', marginBottom:10}}>Estamos en Car</Text>
        </View>
    )
}